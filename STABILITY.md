# قرارداد پایداری دفتریار

> این سند یک یادداشت نیست؛ یک **قرارداد** است. پیش از هر تغییر در این
> پروژه، باید خوانده و رعایت شود.

## چرا این سند وجود دارد

در بازه‌ای کوتاه، این پروژه **۵ نسخه Migration دیتابیس** را پشت سر گذاشت
که بعضی‌شان ساختار داده را عوض کردند. یکی از آن‌ها (نسخه ۵) در واقع رفع
باگی بود که خودِ یک Migration ناقص قبلی ایجاد کرده بود: ستون `systemKey`
اضافه شده بود ولی هرگز پر نشد، و نتیجه‌اش صفر شدن خاموش تمام مانده‌های
مطالبات و پیش‌دریافت بود — خرابی‌ای که هفته‌ها بدون هیچ نشانه‌ای ادامه
داشت.

درس روشن است: **هر تغییر ساختاری یک ریسک جدید است، و ریسک‌های ساختاری
در نرم‌افزار مالی بی‌صدا خراب می‌کنند.**

## تاریخچه Migrationها

| نسخه | تغییر | نوع |
|---|---|---|
| ۲ | ستون `source` برای اسناد + قید یکتایی `systemKey` | ساختاری |
| ۳ | ستون `allowChildren` (تفکیک Leaf-Lock از محافظت هویتی حساب) | ساختاری |
| ۴ | انواع پروژه: ستون تکی ← رابطه چندبه‌چند | ساختاری (پرریسک) |
| ۵ | پرکردن `systemKey` حساب‌های کنترلی | **رفع باگ نسخه ۲** |

## قاعده فعلی: حالت تثبیت

از این نقطه به بعد، پروژه در **حالت تثبیت** است:

### مجاز
- رفع باگ (بدون تغییر Schema)
- بهبود متن، چیدمان، رنگ، پیام خطا
- افزودن تست
- بهبود کارایی که رفتار را عوض نمی‌کند
- افزودن گزارش/نما روی داده‌های موجود

### نیازمند توقف و گفت‌وگو
- هر `ALTER TABLE` یا `CREATE TABLE` جدید
- هر تغییر در معنای یک فیلد موجود
- هر تغییر در فرمول‌های مالی
- هر Migration جدید

**معیار ساده:** اگر تغییر نیاز به بالا بردن `version` دیتابیس دارد، مجاز
نیست مگر برای رفع یک باگ واقعی که داده را خراب می‌کند.

## شرط خروج از حالت تثبیت

حالت تثبیت وقتی پایان می‌یابد که هر سه شرط برقرار شود:

1. **حداقل دو ماه** استفاده واقعی با داده واقعی
2. **آزمون چرخه پشتیبان‌گیری** حداقل یک‌بار با موفقیت انجام شده باشد
   (تنظیمات ← تأیید سلامت پشتیبان)
3. **بدون باگ مالی جدید** در ماه آخر

پس از آن، تغییرات ساختاری دوباره ممکن می‌شوند — ولی یکی‌یکی، نه دسته‌جمعی.

## اگر تغییر ساختاری واقعاً لازم شد

پیش از نوشتن هر کد:

1. **پشتیبان بگیرید** و اثر انگشت داده را یادداشت کنید
2. بنویسید Migration دقیقاً چه کاری با **داده موجود** می‌کند — نه فقط با
   ساختار جدول (باگ نسخه ۲ دقیقاً از همین‌جا آمد: ساختار عوض شد، داده
   دست‌نخورده ماند)
3. یک تست بنویسید که دیتابیس *پیش از* Migration را شبیه‌سازی کند و پس از
   اجرا نتیجه را بسنجد
4. پس از استقرار، بررسی سلامت (بنر داشبورد) را چک کنید

## سازوکارهای محافظ موجود

این سه لایه در پاسخ به باگ‌های واقعی ساخته شده‌اند؛ حذف یا دور زدنشان
مجاز نیست:

- **`MissingControlAccountException`** — نبود حساب کنترلی دیگر با صفر
  خاموش پنهان نمی‌شود
- **`DataHealthService`** — یکپارچگی ساختاری در هر بار باز شدن داشبورد
  بررسی و در صورت مشکل، بنر قرمز نمایش داده می‌شود
- **اثر انگشت داده** — تأیید عددی درستی بازیابی پشتیبان
